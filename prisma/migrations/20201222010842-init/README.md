# Migration `20201222010842-init`

This migration has been generated by Artem Cherenkov at 12/22/2020, 1:08:42 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Issues" (
"actualDueDate" text  NOT NULL ,"client" text  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"department" text  NOT NULL ,"description" text  NOT NULL ,"dueDate" text  NOT NULL ,"id" SERIAL,"isExpired" text  NOT NULL ,"lastAnswer" text  NOT NULL ,"priority" text  NOT NULL ,"product" text  NOT NULL ,"responsible" text  NOT NULL ,"status" text  NOT NULL ,"topic" text  NOT NULL ,"type" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Users" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"name" text  NOT NULL ,"password" text  NOT NULL ,"surname" text  NOT NULL ,
    PRIMARY KEY ("id"))
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201222010842-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,34 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model Issues {
+  id            Int      @default(autoincrement()) @id
+  createdAt     DateTime @default(now())
+  topic         String
+  client        String
+  type          String
+  product       String
+  department    String
+  responsible   String
+  status        String
+  dueDate       String
+  actualDueDate String
+  lastAnswer    String
+  priority      String
+  isExpired     String
+  description   String
+}
+
+model Users {
+  id            Int      @default(autoincrement()) @id
+  createdAt     DateTime @default(now())
+  name      String
+  surname   String
+  password  String
+}
```


